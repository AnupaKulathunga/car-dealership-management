FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# We'll rely on the host generating prisma and having node_modules locally, or we do it here. 
# Better to generate it here to ensure the container architecture matches.
RUN npx prisma generate
EXPOSE 5000
CMD ["npm", "run", "dev"]
